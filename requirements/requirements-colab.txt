# Colab supplies a CUDA-compatible PyTorch build. The version constraint below
# accepts the installed build and avoids replacing it when already satisfied.
numpy>=1.26,<3
pandas>=2.2,<3
pyarrow>=16,<21
scipy>=1.11,<2
scikit-learn>=1.4,<2
torch>=2.4,<3
tqdm>=4.66,<5
pyyaml>=6,<7
matplotlib>=3.8,<4
joblib>=1.3,<2
psutil>=5.9,<7

